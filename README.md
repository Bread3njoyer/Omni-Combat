# Omni-Combat

Currently, this is being run from VS Code using the live server extension. Functionality outside of that will be added later.
The design is simple, using only vanilla HTML, CSS, and JavaScript to accomplish the tasks.
